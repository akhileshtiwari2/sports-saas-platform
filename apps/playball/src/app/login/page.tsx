'use client';

export default function LoginPage() {
  return (
    <div className="min-h-screen bg-[#0a0a0b] flex items-center justify-center p-6">
      <div className="w-full max-w-md rounded-2xl border border-white/10 bg-white/[0.02] p-8">
        <h1 className="text-2xl font-semibold text-white">PLAYBALL Login</h1>
        <p className="mt-2 text-zinc-400">
          Not implemented: login was disabled because it previously used demo credentials only.
        </p>
      </div>
    </div>
  );
}
